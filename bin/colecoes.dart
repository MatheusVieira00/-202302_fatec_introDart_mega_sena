import 'package:colecoes/colecoes.dart' as colecoes;

void main(List<String> arguments) {
  // var nomes = ['ana', 'joao', 'maria'];
  // nomes.add('luan'); //== nomes.insert(nomes.lengh, 'jose');
  // nomes.insert(0, 'lucas');
  // var itensDiversos = [2, 2.5, 'ana'];
  //print(lista.runtimeType);

  // var acumulador = 0;
  // for (final valor in arguments) {
  //   acumulador = acumulador + int.parse(valor);
  // }
  // print(acumulador);
  // try {

  //   int soma = arguments.map((arg) => int.parse(arg)).reduce((value, element) => value + element);

  //   print("Soma dos numeros: $soma");

  // } catch (e) {

  //   print("Erro: input invalido.");

  // }
}
